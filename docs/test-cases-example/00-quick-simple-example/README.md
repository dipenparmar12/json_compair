# Test Case 00: Quick Start (Simple 3-Diff Example)

## Overview
A lightweight, minimal example designed for instant scanning and zero cognitive load.

## The 3 Differences:
1. ✏️ `age`: `30` $\rightarrow$ `31`
2. ✏️ `city`: `"New York"` $\rightarrow$ `"Boston"`
3. ➕ `department`: added `"Engineering"`
